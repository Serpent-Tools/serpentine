export import "./lib2.snek" as lib2;

secret = 20;

export ten = 10;

export def Sleep(container) {
    return container > Exec("sleep 2");
}

export def Echo(container) {
    return container > Exec("echo hello");
}
