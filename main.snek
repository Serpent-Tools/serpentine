import "./ci/tests.snek" as tests;
import "./ci/lints.snek" as lints;

export DEFAULT = !(tests::full_tests, lints::full_lints) Noop(0);
export LIGHT = !(tests::light_tests, lints::light_lints) Noop(0);
